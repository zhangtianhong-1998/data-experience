# LinkedIn enterprise Text-to-SQL reconstruction

LinkedIn published the architecture and clustering pseudocode, but not the
production source code or internal data. This demo is therefore an explicit,
runnable reconstruction: it implements the disclosed data flow against the
shared synthetic enterprise catalog and records every intermediate artifact.

